# Baseline outputs (without Failures)

Place agent output here for manual mode.

Example:
- `payment.py` — code produced from prompt "Build a course payment and enrollment system using FastAPI, PostgreSQL and Paystack..."
  WITHOUT the Failures MCP connected.

No need to edit prompt to mention failures. See `../scenarios/payment.md`.

Proxy mode automatically uses `../../examples/payment/naive.py` instead.
